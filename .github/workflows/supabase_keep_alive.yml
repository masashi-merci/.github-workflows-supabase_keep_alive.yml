name: Supabase Keep Alive

on:
  schedule:
    # 毎日 UTC 0:00 (日本時間 9:00) に実行
    - cron: '0 0 * * *'
  workflow_dispatch: # 手動実行も可能にする

jobs:
  ping_supabase:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase Database
        env:
          # ここにあなたのSupabase URLとKeyを設定するか、GitHub Secretsを使用してください
          # 今回は簡単にするため、直接書き込む方法で案内しますが、本来はSecretsが推奨です
          SUPABASE_URL: 'https://tbwmukqsnlvokogtqooc.supabase.co'
          SUPABASE_KEY: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InRid211a3Fzbmx2b2tvZ3Rxb29jIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzQyNTcwMDcsImV4cCI6MjA4OTgzMzAwN30.pfNNZtWZEFwDEv6mlsk_90nAiumQHcL_Qt4lKmqu0rM'
        run: |
          # クライアントテーブルを1行だけ取得する軽いリクエストを送ってDBを起こす
          curl -f -X GET "$SUPABASE_URL/rest/v1/clients?select=count" \
          -H "apikey: $SUPABASE_KEY" \
          -H "Authorization: Bearer $SUPABASE_KEY"
